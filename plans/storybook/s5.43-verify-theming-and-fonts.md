# S5.43 — Verify S5.41 + S5.42

**Status: Not Started**
**Branch:** journal-updates
**Depends on:** S5.41 + S5.42 (both implemented, needs visual confirmation)

---

## Setup

Start the app: `cd Backend && ./start-services.sh`
Navigate to the Storybook tab. Have at least one published chapter with body text (non-layout), and one chapter with a layout (layout editor chapters).

---

## S5.41 — Extended Book Theming

### Theme picker shows new themes
- [ ] Book theme row shows: **Classic**, **Coral**, **Midnight**, **Meadow** (no Garden, Dusk, or Blush)

### Classic (baseline — nothing should have changed)
- [ ] Select **Classic** → chapter bg is cream `#fdf9f2`
- [ ] Divider shows gold `◆` ornament with gold lines
- [ ] Drop-cap first letter is gold (matches the old behavior)
- [ ] Body text is default dark color

### Coral
- [ ] Select **Coral** → chapter bg turns blush-pink `#fff3f1`
- [ ] Divider shows coral `♡` ornament with coral lines
- [ ] Drop-cap first letter is coral (`#e06c5f`)
- [ ] Body text stays dark (no textColor override)

### Midnight
- [ ] Select **Midnight** → chapter bg turns dark navy `#1e2040`
- [ ] Divider shows silver `·` ornament with silver lines
- [ ] Drop-cap first letter is silver (`#a0a8c8`)
- [ ] Body text is light `#e8eaf6` (readable on dark bg)
- [ ] Theme picker swatch shows the dark bg correctly

### Meadow
- [ ] Select **Meadow** → chapter bg turns sage green `#edf4ed`
- [ ] Divider shows green `✿` ornament with green lines
- [ ] Drop-cap first letter is green (`#4a7a4a`)
- [ ] Body text stays dark

### Layout chapters (LayoutRenderer)
- [ ] Switch themes while viewing a chapter with a layout → header bg + divider update
- [ ] LayoutRenderer canvas itself shows the theme bg for pages without a per-page override
- [ ] Per-page bg color swatches in the layout editor show the correct palette for the active theme

### Page bg swatches in layout editor
- [ ] Open layout editor on any chapter
- [ ] Page bg swatches show 5 shades from the active theme's palette
- [ ] Clicking a swatch changes that page's background

---

## S5.42 — Per-Block Font Selection

### Font picker appears
- [ ] Open layout editor on a chapter with at least one text block
- [ ] Click (single click) a text block → a small dark pill appears at the bottom of the block with **Serif | Sans | Display**
- [ ] Each label renders in its own typeface (Serif = Georgia, Sans = Inter, Display = Poppins)
- [ ] The currently active font has a highlighted background in the picker

### Selecting a font
- [ ] Click **Display** → block text immediately switches to Poppins in the editor
- [ ] Wait 1–2 seconds for auto-save → "Saved ✓" appears
- [ ] Close the editor and reopen → block still shows Poppins (persisted in layout_data)
- [ ] Published view (LayoutRenderer) shows the same Poppins font for that block

### Clearing a font override
- [ ] With Display active on a block, click **Display** again → font reverts to the theme default
- [ ] If theme is Classic (font-serif), the block text goes back to Georgia

### Theme font vs block override
- [ ] Switch book theme to **Meadow** (font-sans) → blocks with no override switch to Inter
- [ ] The block you set to Display stays Poppins (override wins)
- [ ] Switch theme to **Classic** (font-serif) → unoverridden blocks go back to Georgia; overridden block stays Poppins

### Font picker in edit mode
- [ ] Double-click/double-tap a text block to enter edit mode
- [ ] Font picker still visible at bottom of block while editing
- [ ] Clicking a font button while in edit mode changes the font without exiting edit mode
- [ ] Tapping font buttons does NOT accidentally trigger Rnd drag

### Existing chapters without fontFamily
- [ ] Open a chapter saved before S5.42 → blocks render using theme fontClass (no crash, no undefined class)

---

## Regression checks
- [ ] Switching themes updates ALL published chapters on the page simultaneously
- [ ] Draft chapters (amber card) are unaffected by theming changes
- [ ] Milestone/event chapters still render correctly
- [ ] Delete / Edit / "Use classic style" buttons still work on published chapters
