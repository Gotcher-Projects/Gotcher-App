# Social Sharing — Session Entry Prompts

## S1 — Share-Card Foundation
```
We're starting S1 of the social-sharing plan: plans/social-sharing/s1-share-card-foundation.md
Goal: build the DOM→PNG pipeline for shareable image cards — an off-screen html2canvas render util,
a share/download util that attaches a PNG file to the native share sheet (with download + the
existing text/link as fallbacks), and one real card type (first-times) to prove it end to end.
Respect the html2canvas limitations (no pseudo-elements, off-screen fixed-size render). Follow the
plan step by step.
```

## S2 — More Card Types + Theming
```
S1 of the social-sharing plan is complete. We're starting S2: plans/social-sharing/s2-card-types-and-theming.md
Goal: template additional share cards on top of the S1 pipeline — journal entry, pregnancy weekly
size card, and the bump+size card — with light theming so they're on-brand. Follow the plan step
by step.
```
