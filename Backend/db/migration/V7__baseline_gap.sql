-- V7 was intentionally skipped during development (migration was deleted before any database had it applied).
-- This placeholder keeps the Flyway version history contiguous on fresh installs.
SELECT 1;
